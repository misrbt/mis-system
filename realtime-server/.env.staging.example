NODE_ENV=staging
PORT=6001

# >= 32 chars. Generate with: openssl rand -hex 32
REALTIME_SECRET=REPLACE_WITH_32_PLUS_CHAR_RANDOM_STRING

# Staging frontend origin only (no trailing slash, no wildcards)
CORS_ORIGIN=https://staging.mis.local

# Always behind Nginx in staging/prod
TRUST_PROXY=1

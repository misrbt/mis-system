NODE_ENV=production
PORT=6001

# >= 32 chars. Rotate periodically. Generate with: openssl rand -hex 32
REALTIME_SECRET=REPLACE_WITH_32_PLUS_CHAR_RANDOM_STRING

# Production frontend origin only (no trailing slash, no wildcards)
CORS_ORIGIN=https://mis.rbtbank.com

TRUST_PROXY=1

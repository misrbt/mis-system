VITE_API_URL=http://localhost:8000/api

# Realtime Socket.io server (dev only — Node on a separate port).
# Leave UNSET in staging/production: the frontend will connect to the
# current origin, and Nginx proxies /socket.io/ to the Node server.
VITE_REALTIME_URL=http://127.0.0.1:6001

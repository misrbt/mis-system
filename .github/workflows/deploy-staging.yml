name: Deploy to Staging

on:
  push:
    branches: [staging]

jobs:
  deploy:
    name: Deploy MIS to Staging
    runs-on: [self-hosted, staging]
    steps:
      - name: Run staging deploy script
        run: bash /var/www/staging/mis-system/scripts/deploy-staging.sh
